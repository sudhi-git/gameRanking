CREATE TABLE TEAM (TEAMID VARCHAR NOT NULL, OWNER VARCHAR, TEAMNAME VARCHAR, PRIMARY KEY (TEAMID))
CREATE TABLE PLAYERS (PLAYERID VARCHAR NOT NULL, AGE INTEGER, FIRSTNAME VARCHAR, LASTNAME VARCHAR, TEAMID VARCHAR, PRIMARY KEY (PLAYERID))
ALTER TABLE PLAYERS ADD CONSTRAINT FK_PLAYERS_TEAMID FOREIGN KEY (TEAMID) REFERENCES TEAM (TEAMID)
